import { CollectionActivityResponse, SimplifiedCollectionActivity } from "./types";

export const collectionActivityMapper = (activity: CollectionActivityResponse): SimplifiedCollectionActivity => {
  return {};
};
